const $ = (selector) => document.querySelector(selector);
function headers() { const key = $("#adminKey").value.trim(); if (key) sessionStorage.setItem("se-admin-key", key); return key ? { "X-Internal-API-Key": key } : {}; }
async function request(path) { const response = await fetch(path, { headers: headers() }); const body = await response.json().catch(() => null); if (!response.ok) throw new Error(body?.message || `Request failed (${response.status})`); return body?.data ?? body; }
function relative(value) { if (!value) return "Never"; const seconds = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 1000)); if (seconds < 5) return "Just now"; if (seconds < 60) return `${seconds}s ago`; if (seconds < 3600) return `${Math.floor(seconds / 60)}m ago`; return new Date(value).toLocaleString(); }
function toast(message) { const node = $("#toast"); node.textContent = message; node.classList.add("show"); setTimeout(() => node.classList.remove("show"), 2800); }
async function refresh() { const button = $("#refresh"); button.disabled = true; button.textContent = "Refreshing…"; try { const [health, counts, redis] = await Promise.all([request("/health"), request("/api/source/overview"), request("/api/redis/ticks?limit=250")]); const ws = health.websocket || {}; const ready = health.sourceDatabase === "connected" && health.redis?.connected && ws.connected; const receiving = Boolean(ws.lastTickAt) && Date.now() - new Date(ws.lastTickAt).getTime() < 120000; $("#overallState").className = `dashboard-alert ${ready && receiving ? "healthy" : ready ? "warning" : "error"}`; $("#overallTitle").textContent = ready && receiving ? "All systems healthy" : ready ? "Connected, waiting for data" : "A service needs attention"; $("#overallDetail").textContent = ready ? "MySQL, Redis and the provider stream are connected." : "Check the local database, Redis and provider socket."; $("#sideStatus").className = ready ? "online" : "offline"; $("#sideStatusText").textContent = ready ? "Services online" : "Service issue"; $("#competitionCount").textContent = counts.competitions ?? 0; $("#providerEventCount").textContent = counts.events ?? 0; $("#marketCount").textContent = counts.activeMarkets ?? 0; $("#subscriptionCount").textContent = ws.subscribedCount ?? 0; $("#eventCount").textContent = new Set((redis.items || []).map((item) => item.eventId)).size; $("#lastTick").textContent = relative(ws.lastTickAt); $("#updatedAt").textContent = `Updated ${new Date().toLocaleTimeString()} · Refreshes every 15 seconds`; } catch (error) { $("#overallState").className = "dashboard-alert error"; $("#overallTitle").textContent = "Unable to load health"; $("#overallDetail").textContent = error.message; toast(error.message); } finally { button.disabled = false; button.textContent = "Refresh"; } }
$("#adminKey").value = sessionStorage.getItem("se-admin-key") || ""; $("#refresh").addEventListener("click", refresh); void refresh(); setInterval(refresh, 15000);
