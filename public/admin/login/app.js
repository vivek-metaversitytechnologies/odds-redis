const form=document.querySelector("#loginForm"),button=document.querySelector("#submit"),error=document.querySelector("#error");form.addEventListener("submit",async event=>{event.preventDefault();button.disabled=true;button.textContent="Signing in…";error.hidden=true;try{const response=await fetch("/admin/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:document.querySelector("#password").value})}),body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message||"Unable to sign in");const requested=new URLSearchParams(location.search).get("next");location.replace(requested&&requested.startsWith("/admin/")?requested:"/admin/")}catch(e){error.textContent=e.message;error.hidden=false;button.disabled=false;button.textContent="Sign in"}});
